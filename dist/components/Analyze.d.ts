/**
 * Analyze — Codebase analysis with live progress and stats.
 */
import React from 'react';
interface Props {
    projectPath: string;
    provider?: string;
    model?: string;
    persistDir?: string;
    fileTypes?: string;
    embedding?: string;
}
export declare function Analyze({ projectPath, provider, model, persistDir, fileTypes, embedding }: Props): React.ReactElement;
export {};
//# sourceMappingURL=Analyze.d.ts.map