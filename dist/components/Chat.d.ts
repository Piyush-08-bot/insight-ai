/**
 * Chat — Full-screen conversational UI with streaming, command palette, and history.
 */
import React from 'react';
interface Props {
    provider?: string;
    model?: string;
    persistDir?: string;
    initialQuery?: string;
}
export declare function Chat({ provider, model, persistDir, initialQuery }: Props): React.ReactElement;
export {};
//# sourceMappingURL=Chat.d.ts.map